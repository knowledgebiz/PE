import { EvalModelQuantObjective } from './eval-model-quant-objective';

describe('EvalModelQuantObjective', () => {
  it('should create an instance', () => {
    expect(new EvalModelQuantObjective()).toBeTruthy();
  });
});
