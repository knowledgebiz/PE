import { EvalModelCompetency } from './eval-model-competency';

describe('EvalModelCompetency', () => {
  it('should create an instance', () => {
    expect(new EvalModelCompetency()).toBeTruthy();
  });
});
